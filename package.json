{
  "name": "ephemeral-chat",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:signaling": "pnpm --filter signaling dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
